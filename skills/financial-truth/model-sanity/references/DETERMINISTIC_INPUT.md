# Deterministic input

`model_sanity.py` accepts any combination of `probabilities`, `balance_sheet`, ordered `scenarios`, `bounded_metrics`, and `unit_equalities`. It tests supplied invariants; it does not prove a model complete when no relevant checks were supplied.
