# Dale OS — Gemini CLI entry point

Read `AGENTS.md` as the repository operating contract.

If the failure class is already visible, route through `machine/failure-classes.json`. Otherwise use `manifest.json` to choose the narrowest canonical skill under `skills/`.

Do not load or restate the whole operating system. Progressive disclosure is intentional.

Use deterministic tools for mechanical financial truth. Do not upgrade estimates, missing values, or unresolved evidence into authoritative facts.
